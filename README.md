# COre-GOLIAF

Простой шаблон для **лёгкой установки и быстрого старта** проекта.

## 🚀 Быстрый старт

### 1) Клонирование
```bash
git clone https://github.com/<your-org>/COre-GOLIAF.git
cd COre-GOLIAF
```

### 2) Проверка инструментов
Нужны только:
- `git`
- современный shell (`bash`, `zsh` или PowerShell)

Проверка:
```bash
git --version
```

### 3) Готово
Сейчас репозиторий содержит базовую структуру и готов к дальнейшему наполнению.

---

## 🧩 Установка для разработчиков (план)
Когда появится код движка, установка будет выглядеть так:

```bash
# Linux/macOS
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build -j
```

```powershell
# Windows (PowerShell)
cmake -S . -B build -G "Visual Studio 17 2022"
cmake --build build --config Debug
```

> Пока эти команды приведены как шаблон будущей установки.

---

## 📁 Текущая структура
```text
.
├── .gitkeep
└── README.md
```

## ❓ Частые вопросы

**Почему установка такая короткая?**  
Потому что проект пока на начальном этапе и ещё не содержит исходников/зависимостей.

**Что дальше?**  
Добавить CMake-проект, исходники (`src/`), зависимости и CI.
